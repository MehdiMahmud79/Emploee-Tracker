const options=require('./options');
const handleOptionResponse=require('./handleOptionResponse');
let {printToScreen}=require('./dbRequests');

module.exports ={options, printToScreen, handleOptionResponse};